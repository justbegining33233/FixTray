# FixTray internationalization

This note records where UI language lives, why the language control looked gone, which languages were in history, and what this branch adds.

## Languages: what history had vs what was added

Git history never contained a 14- or 16-language list.

| Evidence | What it listed |
| --- | --- |
| `75ad5bb` (Apr 13, 2026), `src/app/superadmin/settings/page.tsx` | English, Spanish, and French. The select was `<option value="en">English</option><option value="es">Spanish</option><option value="fr">French</option>`. |
| `60e5cbb` (Jun 1, 2026, "Restore master code snapshot") | French removed. Catalogs added for `messages/en.json` and `messages/es.json` only. No `messages/fr.json`. |
| Search of settings UI, feature notes, seed data, and language arrays | No other locale enum. `FEATURE_MULTI_LANGUAGE` / `multi_language` defaults off and is not read by the UI. |

French is restored because it was on that original dropdown. The other languages were **not** recovered from the repo. They are the next most common languages spoken at home in the United States (US Census American Community Survey), which is the set a US shop is most likely to need. Native names stay in the menu so a person can find their language before the UI has switched.

| Code | Menu label | In history? |
| --- | --- | --- |
| `en` | English | Yes (`75ad5bb`) |
| `es` | Español | Yes (`75ad5bb`) |
| `fr` | Français | Yes (`75ad5bb`), dropped in `60e5cbb`, restored here |
| `zh` | 中文 | Added |
| `tl` | Tagalog | Added (`fil` normalizes to `tl`) |
| `vi` | Tiếng Việt | Added |
| `ar` | العربية | Added |
| `ko` | 한국어 | Added |
| `ru` | Русский | Added |
| `de` | Deutsch | Added |
| `ht` | Kreyòl Ayisyen | Added |
| `hi` | हिन्दी | Added |
| `pt` | Português | Added |
| `it` | Italiano | Added |
| `pl` | Polski | Added |
| `ur` | اردو | Added |

Layout stays left-to-right for Arabic and Urdu. Right-to-left layout is not implemented.

## How the UI language works

| Piece | Where |
| --- | --- |
| Library | `next-intl`, plugin `createNextIntlPlugin('./i18n.ts')` in `next.config.ts` |
| Request config | `i18n.ts` loads `messages/{locale}.json` for the 16 codes above |
| Resolution | Cookie `fixtray_locale` wins; otherwise `PlatformConfig.defaultLanguage`; otherwise `en`. Rules are in `src/lib/locale.ts` |
| Provider | `src/app/layout.tsx` (`NextIntlClientProvider`, `<html lang>`) |
| Signed-out control | Dropdown in `src/app/auth/layout.tsx` (top right). Replaces the earlier EN/ES buttons |
| Signed-in control | Same dropdown inside the profile menu (`src/components/TopNavBar.tsx`) and the mobile drawer (`src/components/MobileShell.tsx`). There is no separate top-right language control once someone is signed in |
| Platform default | Super Admin → Global Settings and Admin → System Settings. Persisted on `PlatformConfig.defaultLanguage` |

Choosing a language writes the cookie and calls `router.refresh()`. Pages then render the matching catalog. There is no runtime DOM walk and no per-character or live AI overlay.

Two catalog styles share the same JSON files:

- Named keys (`chrome`, `login`, and the older `common` / `auth` / `workorders` / … skeleton) via `useTranslations`.
- English source strings via `usePhrase()` (`src/lib/usePhrase.ts`). The helper looks the English text up in `src/lib/phraseIndex.json` and reads `phrases.<key>` from the active catalog. Next-intl cannot use a sentence as a key because `.` is nesting, so keys are slugs from `src/lib/phraseKey.ts`.

`scripts/generate-locale-catalogs.mjs` scans `say('…')` literals and nearby label fields, merges them into `messages/en.json`, and writes the other 15 catalogs. Translations are generated once and committed. Existing Spanish strings that were already in `messages/es.json` are kept. Brand tokens (`FixTray`, `DVI`, `ZIP`, `SMS`, `DTC`, `KPI`, `MRR`) are shielded so they are not translated. The cache file `messages/.translation-cache.json` is gitignored.

## Why the language option disappeared

1. `next-intl` and the English/Spanish catalogs arrived in `60e5cbb` and were never connected. No page called `useTranslations`. The root layout hardcoded `lang="en"`. `i18n.ts` imported `../messages/...`, which points outside the repo.
2. The only language control was Super Admin → Global Settings → Default Language (`75ad5bb`: English, Spanish, French). The June snapshot dropped French.
3. That dropdown did not stick. `PlatformConfig` had no `defaultLanguage` column, and `PUT /api/admin/settings` ignored the field. After Save, the form snapped back to English.
4. Admin → System Settings never had a language field.
5. `FEATURE_MULTI_LANGUAGE` is not what hid the control. Nothing in the UI reads it.

## Messaging auto-translate

Not implemented, and not part of this pass.

- `DirectMessage` stores `body` only. There is no source language or translation field.
- `GET`/`POST /api/messages` read and write that body as-is.
- Account models have no preferred language. The browser cookie cannot tell the server which language a recipient wants while they are offline.
- There is no translation provider in the app. Catalog generation is a build script, not a message pipeline.

## What is wired in this branch

Copy on these surfaces comes from the catalogs:

- Sign-in, create-account, and password reset (`LoginClient`, `PasswordResetForm`, auth layout dropdown)
- Desktop profile menu and notification chrome (`TopNavBar`), including the language dropdown
- Mobile shell tiles, footer, drawer, and the language dropdown (`MobileShell`)
- Role navigation bars (customer, shop, tech, manager, admin, superadmin)
- Customer dashboard headings, tabs, feature cards, and empty states
- Messaging card chrome (filters, empty state, composer labels). Message bodies are not translated
- Default-language field on system settings and superadmin settings

## What is still English

Most route bodies are still hardcoded. There are on the order of 200 `page.tsx` files. Work-order detail pages, shop/tech/manager home bodies, the rest of the settings forms, and emails are not in the catalogs yet. Dynamic sentences built with template literals (counts, names) stay in English because they are not stable catalog keys. Server error strings that are not also present as `say('…')` literals stay in English. Arabic and Urdu do not switch the layout to right-to-left.

Emails and sender-to-receiver message translation are follow-ups.

## What still needs work

1. Move the remaining page bodies onto `usePhrase` / `useTranslations`, then re-run `node scripts/generate-locale-catalogs.mjs`.
2. Have a person review the machine translations, especially shop terms (work order, tech, DVI) in Tagalog and Haitian Creole.
3. Store a preferred locale on each account before attempting messaging auto-translate. Keep the original `body`, add `sourceLocale` plus a locale → text map, and call a provider with `TRANSLATION_API_KEY`. Do not hardcode keys. If the key is missing, show the original.
4. Right-to-left layout for `ar` and `ur` if those languages stay in the menu.
